# install/

Vendored copies of the skills and hooks, plus the installer. **Do not edit `skills/` or `hooks/`
here** — they are copies of `~/.claude/...`. Change the source, then re-vendor, or the two disagree
silently. See [../docs/agents/what-gets-installed.md](../docs/agents/what-gets-installed.md).
